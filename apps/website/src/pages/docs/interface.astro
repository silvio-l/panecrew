---
import DocsLayout from "../../layouts/DocsLayout.astro";

const title = "Interface Tour | PaneCrew Docs";
const description =
  "A tour of the PaneCrew VS Code extension's interface: the activity-bar explorer, git status decorations, and the terminal grid arranged across VS Code's own editor groups.";

// TODO(screenshots): the earlier version of this page was an interactive,
// numbered-hotspot tour over a real screenshot of the Tauri desktop app's
// window (public/docs/interface-base.png). That screenshot no longer
// matches the product — PaneCrew is a VS Code extension now, not a native
// window — so it's replaced below with a written tour plus an explicit
// placeholder callout. Before this page ships publicly, capture real
// screenshots of the extension running inside VS Code (the PaneCrew
// explorer in the activity bar, a live 2x2 terminal grid, git decorations
// visible on a few files) and either restore a hotspot-style tour over them
// or simply drop them in as plain annotated images — this page's prose
// below already describes what each part does either way.
const stops = [
  {
    title: "PaneCrew explorer",
    body: "A dedicated view in its own activity-bar container — click the PaneCrew icon to open it. One subtree per workspace folder, not one giant merged tree.",
  },
  {
    title: "Git status decorations",
    body: "Modified, added, untracked, and deleted files and folders are badged and colored, based on <code>git status</code> per workspace-folder root.",
  },
  {
    title: "Terminal grid",
    body: "<code>PaneCrew: Open Project Grid…</code> and <code>PaneCrew: Add Folder to Grid…</code> add a workspace folder and give it its own terminal, arranged automatically via VS Code's editor-group layout.",
  },
  {
    title: "A real terminal",
    body: "Every pane is a real, PTY-backed VS Code terminal. Any CLI tool runs here exactly as it would in your own shell — Claude Code, Codex, Gemini CLI, or a plain shell.",
  },
  {
    title: "The focus-following explorer, live",
    body: "Switch to a different terminal or editor tab and the PaneCrew explorer automatically selects, expands, and reveals that pane's owning workspace folder — no manual re-navigation.",
  },
  {
    title: "Compact Look",
    body: "<code>PaneCrew: Apply Compact Look</code> hides secondary chrome (status bar, minimap by default) to make room for more panes. The activity bar always stays visible, by design.",
  },
];
---
<DocsLayout title={title} description={description} path="/docs/interface/" active="interface">
  <h1>Interface tour</h1>
  <p class="lede">
    PaneCrew adds two things to VS Code: an explorer that follows your focus, and a grid of
    terminals arranged across your workspace. Here's what each part does.
  </p>

  <div class="shot-placeholder" role="img" aria-label="Placeholder for a real PaneCrew screenshot, not yet captured">
    <svg viewBox="0 0 24 24" aria-hidden="true" width="28" height="28"><path fill="currentColor" d="M4 5h16a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1Zm1 2v10h14V7H5Zm2 8 3.5-4.5 2.5 3L16 9l4 6H7Z"/></svg>
    <p><b>Screenshot coming soon.</b> This page will carry real, current screenshots of the extension running inside VS Code once they're captured.</p>
  </div>

  <div class="stops">
    {stops.map((s, i) => (
      <div class="stop">
        <span class="stop-idx" aria-hidden="true">{i + 1}</span>
        <div>
          <h3>{s.title}</h3>
          <p set:html={s.body} />
        </div>
      </div>
    ))}
  </div>
</DocsLayout>

<style>
  .shot-placeholder {
    display: flex; align-items: center; gap: 16px;
    margin: 8px 0 40px; padding: 22px 24px;
    border: 1px dashed var(--line-dark); border-radius: 12px;
    background: rgba(255, 253, 245, 0.04);
    color: rgba(255, 253, 245, 0.7);
    max-width: 720px;
  }
  .shot-placeholder svg { flex: none; color: var(--amber); }
  .shot-placeholder p { margin: 0; text-wrap: pretty; font-size: 0.9375rem; }
  .shot-placeholder b { color: var(--cream); }

  .stops { display: flex; flex-direction: column; gap: 22px; max-width: 720px; }
  .stop { display: flex; gap: 14px; }
  .stop-idx {
    flex: none; width: 26px; height: 26px; margin-top: 2px;
    display: grid; place-items: center; border-radius: 50%;
    font-family: var(--mono); font-size: 0.8125rem; font-weight: 700;
    color: var(--indigo-deep); background: var(--amber);
  }
  .stop h3 {
    font-family: var(--display); font-weight: 700; font-size: 1.0625rem;
    color: var(--cream); margin-bottom: 4px;
  }
  .stop p { color: rgba(255, 253, 245, 0.82); text-wrap: pretty; margin: 0; }
  .stop p :global(code) {
    font-family: var(--mono); font-size: 0.875em;
    background: rgba(255, 253, 245, 0.08); border-radius: 4px; padding: 0.1em 0.4em;
  }
</style>
