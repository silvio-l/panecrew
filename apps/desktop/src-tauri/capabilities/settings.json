{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the settings window. `core:window:allow-hide` is what lets the window dismiss itself from JS (see SettingsWindow.tsx's onboarding-restart button); `allow-close` is deliberately NOT granted, because this window is a prewarmed singleton that must only ever be hidden, never destroyed (settings_window.rs).",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "log:default",
    "core:window:allow-hide",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "x-apple.systempreferences:*" }]
    }
  ]
}
