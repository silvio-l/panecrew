# AI instruction / internal planning files — never public (see dev-standards
# OSS-Allowlist policy: internals stay local, only .ossallowlist paths ship).
CLAUDE.md
CONTEXT.md
docs/decisions.md
docs/adr/
docs/agents/
.scratch/
PRODUCT.md
DESIGN.md
.impeccable/
tools/

# Secrets
.env
.env.local

# Node / pnpm workspace
node_modules/
dist/
dist-ssr/

# Promo-Pipeline: Storyboard, das die Capture-Stage zur Laufzeit an den
# Demo-Harness ausliefert (tools/promo-pipeline/capture.py) — generiert pro
# Lauf, kein Quelltext.
apps/desktop/public/harness-storyboards/*
!apps/desktop/public/harness-storyboards/.gitkeep

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
